<%= form_with(model: dessert) do |form| %>
  <% if dessert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dessert.errors.count, "error") %> prohibited this dessert from being saved:</h2>

      <ul>
        <% dessert.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :"Recipe Name" %>
    <%= form.text_field :rname %>
  </div>
<div class="field">
  <%= form.label :image %>
  <%= form.file_field :image %>
</div>

  <div class="field">
    <%= form.label :ingredient %>
    <%= form.text_area :ingredient %>
  </div>

  <div class="field">
    <%= form.label :recipe %>
    <%= form.text_area :method %>
  </div>

  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
